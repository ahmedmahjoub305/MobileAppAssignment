<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="mmu.edu.customerinterface">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomerInterface">
        <activity android:name=".Activities.SignIn"/>
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.WorkOrRequest" />
        <activity android:name=".Activities.HowToActivity3" />
        <activity android:name=".Activities.HowToActivity2" />
        <activity android:name=".Activities.HowToActivity1" />
        <activity android:name=".Activities.WelcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>